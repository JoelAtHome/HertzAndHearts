# Local QRS theory source (Word)

Place **`Intro to QRS Cardiac Waveform rev 15MAR2026 - Reorganized.docx`** here (or let `python docs/cardiac_md_export.py` copy it once from the OneDrive path defined in `docs/cardiac_md_export.py` if this file is missing).

Markdown export reads this file first, then falls back to OneDrive only if this copy is absent.
